# 评测深度止步 L2：并排展示 + 客观指标，不做投票与自动判分

MVP 的「效果对比」只包含并排展示（轨迹直播 + 最终 diff）与客观指标（耗时、token、成本）。不做人级投票/ELO 榜单（个人工具没有对战量，冷启动榜单无说服力，LMArena 已用 1 亿美元融资做这件事），也不做 test.sh 客观判分或 LLM-as-judge（前者需要题库工程，后者引入裁判偏差）。可信度由「一键重跑看方差」承担。
